#include <iostream>

int main() {
    std::cout << "Running tests..." << std::endl;
    // For now, our test is just to ensure the program runs without crashing.
    // In the future, we will add a proper testing framework.
    std::cout << "Tests passed!" << std::endl;
    return 0;
}
