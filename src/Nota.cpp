#include "Nota.h"
#include <iostream>

namespace nota {
    void Nota::run_file(const std::string& path) {
        // In the future, this will read the file and compile it.
        std::cout << "Running file: " << path << std::endl;
    }
}
